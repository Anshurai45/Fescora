import SectionTitle from './SectionTitle'; import { industries } from '../data/content'
export default function Industries() { return <section className="section industries"><div className="container"><SectionTitle eyebrow="Industries We Serve" title="Workforce solutions adapted to your operating environment." text="Different industries have different workforce, security and facility requirements. Our service approach can be structured around the needs of each operating environment." /><div className="industry-grid">{industries.map(([name, Icon]) => <div key={name}><Icon size={24}/><span>{name}</span></div>)}</div></div></section> }