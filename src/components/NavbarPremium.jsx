import { useEffect, useState } from 'react'; import { Link, NavLink } from 'react-router-dom'; import { Menu, X } from 'lucide-react'; import BrandLockup from './BrandLockup'
const links = [['Home', '/'], ['About', '/about'], ['Services', '/services'], ['Industries', '/industries'], ['Careers', '/careers'], ['Register', '/candidate-registration'], ['Contact', '/contact']]
export default function NavbarPremium() { const [open, setOpen] = useState(false), [scrolled, setScrolled] = useState(false); useEffect(() => { const onScroll = () => setScrolled(scrollY > 20); addEventListener('scroll', onScroll); return () => removeEventListener('scroll', onScroll) }, []); return <header className={`navbar ${scrolled ? 'is-scrolled' : ''}`}><div className="nav-inner"><Link to="/" className="logo" onClick={() => setOpen(false)} aria-label="Fescora Management home"><BrandLockup/></Link><button className="menu-button" onClick={() => setOpen(!open)} aria-label="Toggle navigation">{open ? <X/> : <Menu/>}</button><nav className={open ? 'open' : ''}>{links.map(([name, path]) => <NavLink key={name} to={path} onClick={() => setOpen(false)}>{name}</NavLink>)}</nav><Link className="nav-cta desktop-cta" to="/contact">Let's talk</Link></div></header> }
