import { Quote } from 'lucide-react'; import SectionTitle from './SectionTitle'; import { testimonials } from '../data/content'
export default function Testimonials() { return <section className="section"><div className="container"><SectionTitle eyebrow="Client Experience" title="Service built around dependable execution." text="We focus on responsive communication, consistent service delivery and practical solutions that support the day-to-day requirements of our clients." /><div className="testimonial-grid">{testimonials.map(t => <article className="testimonial" key={t.name}><Quote /><strong>{t.name}</strong><small>{t.role}</small></article>)}</div></div></section> }