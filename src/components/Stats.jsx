import { motion } from "framer-motion";
const standards = [["Reliable Service Teams", "Service teams focused on the practical needs of each site."], ["Clear Processes", "Defined responsibilities and coordination for day-to-day operations."], ["Responsive Coordination", "Regular communication to help keep service delivery organised."], ["Operational Support", "Workforce and facility support that can adapt to changing requirements."]];
export default function Stats() { return <section className="stats operating-standards"><div className="container"><div className="standards-heading"><span>How we operate</span><h2>Practical support for essential operations.</h2></div><div className="standards-grid">{standards.map(([title, text], index) => <motion.article key={title} initial={{ opacity: 0, y: 15 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * 0.06 }}><b>0{index + 1}</b><h3>{title}</h3><p>{text}</p></motion.article>)}</div></div></section>; }